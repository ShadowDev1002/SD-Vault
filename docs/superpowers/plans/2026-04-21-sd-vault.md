# SD-Vault Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build SD-Vault — ein hochsicherer, lokaler Passwort-Manager mit Dual-Key-Verschlüsselung (Argon2id + XChaCha20-Poly1305), SQLCipher-Datenbank und SFTP-Sync.

**Architecture:** Tauri v2 App. Rust-Backend übernimmt alle Kryptographie und Datenbankzugriffe. React/TypeScript Frontend kommuniziert via Tauri IPC. Master Key wird aus Master-Passwort + Secret Key (via BLAKE3 + Argon2id) abgeleitet und **niemals** auf Disk geschrieben. Doppelschicht: SQLCipher für die DB-Datei + XChaCha20-Poly1305 pro Eintrag.

**Tech Stack:** Rust, Tauri v2, React 18, TypeScript, Tailwind CSS v3, SQLCipher (rusqlite), Argon2id, XChaCha20-Poly1305, BLAKE3, HKDF-SHA256, zeroize, ssh2, printpdf, pnpm

---

## File Map

### Rust Backend (`src-tauri/`)
| Datei | Verantwortung |
|---|---|
| `Cargo.toml` | Alle Rust-Abhängigkeiten |
| `build.rs` | Tauri Build-Script |
| `tauri.conf.json` | Tauri-Konfiguration |
| `capabilities/default.json` | Tauri v2 Permissions |
| `src/main.rs` | Einstiegspunkt → ruft `run()` |
| `src/lib.rs` | AppState + alle Tauri-Commands |
| `src/crypto.rs` | Argon2id, BLAKE3, HKDF, XChaCha20-Poly1305, Secret Key Gen |
| `src/db.rs` | SQLCipher öffnen, Schema, CRUD |
| `src/emergency_kit.rs` | PDF-Generierung (printpdf) |
| `src/sync/mod.rs` | `SyncProvider` Trait + `BackupEntry` Typ |
| `src/sync/config.rs` | `SftpConfig` + `SyncConfig` TOML-Serialisierung |
| `src/sync/local.rs` | LocalBackupProvider (max. 10 Backups) |
| `src/sync/sftp.rs` | SftpProvider (ssh2) |
| `src/sync/google_drive.rs` | GoogleDriveProvider Stub |

### Frontend (`src/`)
| Datei | Verantwortung |
|---|---|
| `src/types.ts` | TypeScript-Typen passend zu Rust-Structs |
| `src/utils/clipboard.ts` | Auto-clear Clipboard nach 30s |
| `src/utils/strength.ts` | Passwort-Stärke via zxcvbn |
| `src/main.tsx` | React-Einstiegspunkt |
| `src/App.tsx` | Auth-State + Screen-Router |
| `src/components/LockScreen.tsx` | Unlock-Formular (Master PW + Secret Key) |
| `src/components/FirstRunSetup.tsx` | Neuen Vault erstellen + Emergency Kit Download |
| `src/components/VaultView.tsx` | Hauptlayout nach Unlock |
| `src/components/Sidebar.tsx` | Kategorie-Navigation + Suchfeld |
| `src/components/EntryList.tsx` | Gefilterte Eintrags-Liste |
| `src/components/EntryDetail.tsx` | Eintrag ansehen/bearbeiten |
| `src/components/Settings.tsx` | Settings-Container |
| `src/components/SecuritySettings.tsx` | Auto-Lock Timeout |
| `src/components/SyncSettings.tsx` | Local Backup + SFTP konfigurieren |

---

## Task 1: Project Scaffolding

**Files:**
- Create: `src-tauri/Cargo.toml`
- Create: `src-tauri/build.rs`
- Create: `src-tauri/tauri.conf.json`
- Create: `src-tauri/capabilities/default.json`
- Create: `package.json`
- Create: `index.html`
- Create: `vite.config.ts`
- Create: `tailwind.config.js`
- Create: `postcss.config.js`
- Create: `tsconfig.json`

- [ ] **Step 1: Verzeichnisstruktur anlegen**

```bash
mkdir -p src-tauri/src/sync src-tauri/icons src-tauri/capabilities src/components src/utils
```

- [ ] **Step 2: `src-tauri/Cargo.toml` erstellen**

```toml
[package]
name = "sd-vault"
version = "0.1.0"
edition = "2021"

[lib]
name = "sd_vault_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.31", features = ["bundled-sqlcipher-vendored-openssl"] }
argon2 = "0.5"
chacha20poly1305 = "0.10"
blake3 = "1"
hkdf = "0.12"
sha2 = "0.10"
zeroize = { version = "1", features = ["derive"] }
ssh2 = "0.9"
dirs = "5"
uuid = { version = "1", features = ["v4"] }
tokio = { version = "1", features = ["full"] }
printpdf = "0.7"
base32 = "0.4"
rand = "0.8"
async-trait = "0.1"
hex = "0.4"
toml = "0.8"
chrono = { version = "0.4", features = ["serde"] }
```

- [ ] **Step 3: `src-tauri/build.rs` erstellen**

```rust
fn main() {
    tauri_build::build()
}
```

- [ ] **Step 4: `src-tauri/tauri.conf.json` erstellen**

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "SD-Vault",
  "version": "0.1.0",
  "identifier": "de.shadowdev.sd-vault",
  "build": {
    "frontendDist": "../dist",
    "devUrl": "http://localhost:1420",
    "beforeDevCommand": "pnpm dev",
    "beforeBuildCommand": "pnpm build"
  },
  "app": {
    "windows": [
      {
        "title": "SD-Vault",
        "width": 1200,
        "height": 800,
        "minWidth": 800,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

- [ ] **Step 5: `src-tauri/capabilities/default.json` erstellen**

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability set for SD-Vault",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save"
  ]
}
```

- [ ] **Step 6: `package.json` erstellen**

```json
{
  "name": "sd-vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zxcvbn": "^4.4.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/zxcvbn": "^4.4.4",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.4.5",
    "vite": "^5.3.1"
  }
}
```

- [ ] **Step 7: `index.html` erstellen**

```html
<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SD-Vault</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 8: `vite.config.ts` erstellen**

```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  clearScreen: false,
  server: {
    port: 1420,
    strictPort: true,
    watch: { ignored: ["**/src-tauri/**"] },
  },
});
```

- [ ] **Step 9: `tailwind.config.js` erstellen**

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        vault: {
          bg: "#0f1117",
          surface: "#1a1d27",
          border: "#2a2d3a",
          accent: "#6366f1",
          danger: "#ef4444",
          muted: "#6b7280",
        },
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 10: `postcss.config.js` erstellen**

```javascript
export default {
  plugins: { tailwindcss: {}, autoprefixer: {} },
};
```

- [ ] **Step 11: `tsconfig.json` erstellen**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true
  },
  "include": ["src"]
}
```

- [ ] **Step 12: Frontend-Abhängigkeiten installieren**

```bash
pnpm install
```

Expected: `node_modules/` wird angelegt, kein Fehler

- [ ] **Step 13: Commit**

```bash
git add -A
git commit -m "chore: scaffold SD-Vault project structure"
```

---

## Task 2: Crypto Module

**Files:**
- Create: `src-tauri/src/crypto.rs`
- Create: `src-tauri/src/lib.rs` (temporär für Compile-Check)

- [ ] **Step 1: `src-tauri/src/crypto.rs` erstellen**

```rust
use argon2::{Algorithm, Argon2, Params, Version};
use blake3::Hasher;
use chacha20poly1305::{
    aead::{Aead, AeadCore, KeyInit},
    XChaCha20Poly1305, XNonce,
};
use hkdf::Hkdf;
use rand::RngCore;
use rand::rngs::OsRng;
use sha2::Sha256;
use zeroize::Zeroizing;

pub const KEY_LEN: usize = 32;
pub const ARGON2_MEM_KB: u32 = 256 * 1024; // 256 MB
pub const ARGON2_OPS: u32 = 3;
pub const ARGON2_THREADS: u32 = 4;

/// Leitet einen 32-Byte Master Key ab aus: Master-Passwort + Secret Key (via BLAKE3) → Argon2id.
/// `salt` muss in vault.salt neben der DB gespeichert werden (Bootstrap-Lösung).
pub fn derive_master_key(
    master_pw: &str,
    secret_key_bytes: &[u8],
    salt: &[u8; 32],
) -> Result<Zeroizing<[u8; KEY_LEN]>, String> {
    let mut hasher = Hasher::new();
    hasher.update(master_pw.as_bytes());
    hasher.update(b"||");
    hasher.update(secret_key_bytes);
    let combined = hasher.finalize();

    let params = Params::new(ARGON2_MEM_KB, ARGON2_OPS, ARGON2_THREADS, Some(KEY_LEN))
        .map_err(|e| e.to_string())?;
    let argon2 = Argon2::new(Algorithm::Argon2id, Version::V0x13, params);

    let mut key = Zeroizing::new([0u8; KEY_LEN]);
    argon2
        .hash_password_into(combined.as_bytes(), salt, key.as_mut())
        .map_err(|e| e.to_string())?;

    Ok(key)
}

/// Leitet den SQLCipher-Key via HKDF-SHA256 aus dem Master Key ab.
pub fn derive_sqlcipher_key(master_key: &[u8; KEY_LEN]) -> Zeroizing<[u8; KEY_LEN]> {
    let hk = Hkdf::<Sha256>::new(None, master_key);
    let mut okm = Zeroizing::new([0u8; KEY_LEN]);
    hk.expand(b"sd-vault:sqlcipher:v1", okm.as_mut()).unwrap();
    okm
}

/// Leitet den Entry-Encryption-Key via HKDF-SHA256 aus dem Master Key ab.
pub fn derive_entry_key(master_key: &[u8; KEY_LEN]) -> Zeroizing<[u8; KEY_LEN]> {
    let hk = Hkdf::<Sha256>::new(None, master_key);
    let mut okm = Zeroizing::new([0u8; KEY_LEN]);
    hk.expand(b"sd-vault:entries:v1", okm.as_mut()).unwrap();
    okm
}

/// Verschlüsselt plaintext mit XChaCha20-Poly1305. Gibt nonce (24 Byte) || ciphertext zurück.
pub fn encrypt(key: &[u8; KEY_LEN], plaintext: &[u8]) -> Result<Vec<u8>, String> {
    let cipher = XChaCha20Poly1305::new_from_slice(key).map_err(|e| e.to_string())?;
    let nonce = XChaCha20Poly1305::generate_nonce(&mut OsRng);
    let ciphertext = cipher
        .encrypt(&nonce, plaintext)
        .map_err(|_| "Encryption failed".to_string())?;

    let mut result = Vec::with_capacity(24 + ciphertext.len());
    result.extend_from_slice(&nonce);
    result.extend_from_slice(&ciphertext);
    Ok(result)
}

/// Entschlüsselt Daten aus `encrypt`. Erwartet nonce (24 Byte) || ciphertext.
pub fn decrypt(key: &[u8; KEY_LEN], data: &[u8]) -> Result<Vec<u8>, String> {
    if data.len() < 24 {
        return Err("Ciphertext zu kurz".into());
    }
    let (nonce_bytes, ciphertext) = data.split_at(24);
    let nonce = XNonce::from_slice(nonce_bytes);
    let cipher = XChaCha20Poly1305::new_from_slice(key).map_err(|e| e.to_string())?;
    cipher
        .decrypt(nonce, ciphertext)
        .map_err(|_| "Entschlüsselung fehlgeschlagen — falscher Key oder korrupte Daten".into())
}

/// Generiert einen 20-Byte (160-Bit) Secret Key.
/// Gibt (raw_bytes, formatted_string) zurück.
/// Format: "SDVLT-XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX" (Base32, 4 × 8 Zeichen = 32 Chars)
pub fn generate_secret_key() -> ([u8; 20], String) {
    let mut bytes = [0u8; 20];
    OsRng.fill_bytes(&mut bytes);
    let encoded = base32::encode(base32::Alphabet::RFC4648 { padding: false }, &bytes);
    // 20 Byte × 8 / 5 = 32 Base32-Chars exakt
    let formatted = format!(
        "SDVLT-{}-{}-{}-{}",
        &encoded[0..8],
        &encoded[8..16],
        &encoded[16..24],
        &encoded[24..32]
    );
    (bytes, formatted)
}

/// Parst einen formatierten Secret Key zurück zu raw bytes.
/// Akzeptiert: "SDVLT-XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX"
pub fn parse_secret_key(formatted: &str) -> Result<[u8; 20], String> {
    let stripped = formatted
        .strip_prefix("SDVLT-")
        .ok_or("Ungültiges Format: fehlendes 'SDVLT-' Präfix")?
        .replace('-', "");

    let bytes = base32::decode(base32::Alphabet::RFC4648 { padding: false }, &stripped)
        .ok_or("Ungültiger Secret Key: kein gültiges Base32")?;

    if bytes.len() != 20 {
        return Err(format!(
            "Ungültige Secret Key Länge: erwartet 20 Bytes, erhalten {}",
            bytes.len()
        ));
    }

    let mut arr = [0u8; 20];
    arr.copy_from_slice(&bytes);
    Ok(arr)
}

/// Generiert einen zufälligen 32-Byte Argon2-Salt.
pub fn generate_salt() -> [u8; 32] {
    let mut salt = [0u8; 32];
    OsRng.fill_bytes(&mut salt);
    salt
}
```

- [ ] **Step 2: Temporäres `src-tauri/src/lib.rs` für Compile-Check**

```rust
pub mod crypto;
```

- [ ] **Step 3: Kompilierung prüfen**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

Expected: Kompiliert ohne Fehler (Warnings über ungenutzte Items sind ok)

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/crypto.rs src-tauri/src/lib.rs
git commit -m "feat: add crypto module (Argon2id + BLAKE3 + HKDF + XChaCha20-Poly1305)"
```

---

## Task 3: Database Module

**Files:**
- Create: `src-tauri/src/db.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: `src-tauri/src/db.rs` erstellen**

```rust
use chrono::Utc;
use rusqlite::{params, Connection};
use serde::{Deserialize, Serialize};
use sha2::{Digest, Sha256};
use std::path::Path;
use uuid::Uuid;

use crate::crypto;

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct VaultMeta {
    pub id: String,
    pub created_at: i64,
    pub argon2_mem: u32,
    pub argon2_ops: u32,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct ItemWithPayload {
    pub id: String,
    pub category: String,
    pub updated_at: i64,
    pub payload: ItemPayload,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct ItemPayload {
    pub title: String,
    pub username: String,
    pub password: String,
    pub url: String,
    pub notes: String,
    #[serde(default)]
    pub fields: Vec<CustomField>,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct CustomField {
    pub label: String,
    pub value: String,
}

/// Öffnet (oder erstellt) eine SQLCipher-Datenbank. Der Key MUSS gesetzt werden
/// bevor irgendeine andere Operation ausgeführt wird.
pub fn open_db(path: &Path, sqlcipher_key: &[u8; 32]) -> Result<Connection, String> {
    let conn = Connection::open(path).map_err(|e| e.to_string())?;

    let key_hex = hex::encode(sqlcipher_key.as_ref());
    conn.execute_batch(&format!("PRAGMA key = \"x'{}'\";\n", key_hex))
        .map_err(|e| format!("SQLCipher Key-Fehler: {}", e))?;

    // Verify: Query schlägt fehl wenn Key falsch
    conn.execute_batch("SELECT count(*) FROM sqlite_master;")
        .map_err(|_| "Falscher Master Key oder korrupte Datenbank".to_string())?;

    ensure_schema(&conn)?;
    Ok(conn)
}

fn ensure_schema(conn: &Connection) -> Result<(), String> {
    conn.execute_batch(
        "CREATE TABLE IF NOT EXISTS vault_meta (
            id           TEXT PRIMARY KEY,
            created_at   INTEGER NOT NULL,
            argon2_salt  BLOB NOT NULL,
            argon2_ops   INTEGER NOT NULL,
            argon2_mem   INTEGER NOT NULL,
            version      INTEGER NOT NULL DEFAULT 1
        );
        CREATE TABLE IF NOT EXISTS items (
            id              TEXT PRIMARY KEY,
            category        TEXT NOT NULL,
            updated_at      INTEGER NOT NULL,
            sync_hash       TEXT NOT NULL,
            encrypted_blob  BLOB NOT NULL
        );",
    )
    .map_err(|e| e.to_string())
}

/// Legt einen neuen vault_meta-Eintrag an. Nur einmal beim Vault-Erstellen aufrufen.
pub fn init_vault_meta(
    conn: &Connection,
    vault_id: &str,
    salt: &[u8; 32],
) -> Result<VaultMeta, String> {
    let now = Utc::now().timestamp();
    conn.execute(
        "INSERT INTO vault_meta (id, created_at, argon2_salt, argon2_ops, argon2_mem, version)
         VALUES (?1, ?2, ?3, ?4, ?5, 1)",
        params![vault_id, now, salt.as_ref(), crypto::ARGON2_OPS, crypto::ARGON2_MEM_KB],
    )
    .map_err(|e| e.to_string())?;

    Ok(VaultMeta {
        id: vault_id.to_string(),
        created_at: now,
        argon2_mem: crypto::ARGON2_MEM_KB,
        argon2_ops: crypto::ARGON2_OPS,
    })
}

/// Liest vault_meta (für die UI nach dem Unlock).
pub fn read_vault_meta(conn: &Connection) -> Result<VaultMeta, String> {
    conn.query_row(
        "SELECT id, created_at, argon2_ops, argon2_mem FROM vault_meta LIMIT 1",
        [],
        |row| {
            Ok(VaultMeta {
                id: row.get(0)?,
                created_at: row.get(1)?,
                argon2_ops: row.get(2)?,
                argon2_mem: row.get(3)?,
            })
        },
    )
    .map_err(|e| e.to_string())
}

/// Fügt einen verschlüsselten Eintrag ein. Gibt die neue ID zurück.
pub fn insert_item(
    conn: &Connection,
    entry_key: &[u8; 32],
    category: &str,
    payload: &ItemPayload,
) -> Result<String, String> {
    let id = Uuid::new_v4().to_string();
    let now = Utc::now().timestamp();
    let json = serde_json::to_vec(payload).map_err(|e| e.to_string())?;
    let blob = crypto::encrypt(entry_key, &json)?;
    let hash = hex::encode(Sha256::digest(&blob));

    conn.execute(
        "INSERT INTO items (id, category, updated_at, sync_hash, encrypted_blob)
         VALUES (?1, ?2, ?3, ?4, ?5)",
        params![id, category, now, hash, blob],
    )
    .map_err(|e| e.to_string())?;

    Ok(id)
}

/// Aktualisiert den verschlüsselten Payload eines Eintrags.
pub fn update_item(
    conn: &Connection,
    entry_key: &[u8; 32],
    id: &str,
    payload: &ItemPayload,
) -> Result<(), String> {
    let now = Utc::now().timestamp();
    let json = serde_json::to_vec(payload).map_err(|e| e.to_string())?;
    let blob = crypto::encrypt(entry_key, &json)?;
    let hash = hex::encode(Sha256::digest(&blob));

    conn.execute(
        "UPDATE items SET updated_at = ?1, sync_hash = ?2, encrypted_blob = ?3 WHERE id = ?4",
        params![now, hash, blob, id],
    )
    .map_err(|e| e.to_string())?;

    Ok(())
}

/// Löscht einen Eintrag nach ID.
pub fn delete_item(conn: &Connection, id: &str) -> Result<(), String> {
    conn.execute("DELETE FROM items WHERE id = ?1", params![id])
        .map_err(|e| e.to_string())?;
    Ok(())
}

/// Gibt alle Einträge entschlüsselt zurück, optional nach Kategorie gefiltert.
pub fn get_items(
    conn: &Connection,
    entry_key: &[u8; 32],
    category: Option<&str>,
) -> Result<Vec<ItemWithPayload>, String> {
    let sql_all = "SELECT id, category, updated_at, encrypted_blob FROM items ORDER BY updated_at DESC";
    let sql_cat = "SELECT id, category, updated_at, encrypted_blob FROM items WHERE category = ?1 ORDER BY updated_at DESC";

    let mut stmt = match category {
        Some(_) => conn.prepare(sql_cat),
        None => conn.prepare(sql_all),
    }
    .map_err(|e| e.to_string())?;

    let mut rows = match category {
        Some(cat) => stmt.query(params![cat]),
        None => stmt.query([]),
    }
    .map_err(|e| e.to_string())?;

    let mut items = Vec::new();
    while let Some(row) = rows.next().map_err(|e| e.to_string())? {
        let blob: Vec<u8> = row.get(3).map_err(|e| e.to_string())?;
        let plaintext = crypto::decrypt(entry_key, &blob)?;
        let payload: ItemPayload =
            serde_json::from_slice(&plaintext).map_err(|e| e.to_string())?;

        items.push(ItemWithPayload {
            id: row.get(0).map_err(|e| e.to_string())?,
            category: row.get(1).map_err(|e| e.to_string())?,
            updated_at: row.get(2).map_err(|e| e.to_string())?,
            payload,
        });
    }

    Ok(items)
}
```

- [ ] **Step 2: `src-tauri/src/lib.rs` aktualisieren**

```rust
pub mod crypto;
pub mod db;
```

- [ ] **Step 3: Kompilierung prüfen**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

Expected: Kompiliert ohne Fehler

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/db.rs src-tauri/src/lib.rs
git commit -m "feat: add database module (SQLCipher + schema + CRUD)"
```

---

## Task 4: Emergency Kit PDF

**Files:**
- Create: `src-tauri/src/emergency_kit.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: `src-tauri/src/emergency_kit.rs` erstellen**

```rust
use printpdf::*;
use std::io::BufWriter;

/// Generiert ein Emergency Kit PDF und gibt die rohen PDF-Bytes zurück.
pub fn generate_pdf(
    vault_id: &str,
    secret_key_formatted: &str,
    created_at: &str,
) -> Result<Vec<u8>, String> {
    let (doc, page1, layer1) =
        PdfDocument::new("SD-Vault Emergency Kit", Mm(210.0), Mm(297.0), "Layer 1");
    let layer = doc.get_page(page1).get_layer(layer1);

    let font_bold = doc
        .add_builtin_font(BuiltinFont::HelveticaBold)
        .map_err(|e| e.to_string())?;
    let font = doc
        .add_builtin_font(BuiltinFont::Helvetica)
        .map_err(|e| e.to_string())?;
    let font_mono = doc
        .add_builtin_font(BuiltinFont::Courier)
        .map_err(|e| e.to_string())?;

    layer.use_text("SD-Vault Emergency Kit", 24.0, Mm(20.0), Mm(265.0), &font_bold);

    // Trennlinie
    let line = Line {
        points: vec![
            (Point::new(Mm(20.0), Mm(258.0)), false),
            (Point::new(Mm(190.0), Mm(258.0)), false),
        ],
        is_closed: false,
    };
    layer.set_outline_color(Color::Rgb(Rgb::new(0.4, 0.4, 0.4, None)));
    layer.set_outline_thickness(0.5);
    layer.add_line(line);

    layer.use_text(
        "WICHTIG: Ohne diesen Secret Key und Ihr Master-Passwort",
        11.0, Mm(20.0), Mm(248.0), &font_bold,
    );
    layer.use_text(
        "kann Ihr Vault NICHT geoeffnet werden. Sicher aufbewahren.",
        11.0, Mm(20.0), Mm(241.0), &font_bold,
    );

    layer.use_text("Vault-ID:", 10.0, Mm(20.0), Mm(224.0), &font_bold);
    layer.use_text(vault_id, 10.0, Mm(55.0), Mm(224.0), &font_mono);

    layer.use_text("Secret Key:", 10.0, Mm(20.0), Mm(210.0), &font_bold);
    layer.use_text(secret_key_formatted, 14.0, Mm(20.0), Mm(200.0), &font_mono);

    layer.use_text("Erstellt am:", 10.0, Mm(20.0), Mm(184.0), &font_bold);
    layer.use_text(created_at, 10.0, Mm(55.0), Mm(184.0), &font);

    layer.use_text("So entsperren Sie Ihren Vault:", 11.0, Mm(20.0), Mm(168.0), &font_bold);
    let steps = [
        "1. SD-Vault starten und 'Vault oeffnen' waehlen.",
        "2. Ihr Master-Passwort eingeben.",
        "3. Den Secret Key aus diesem Dokument eingeben.",
        "4. Ihr Vault wird entschluesselt und geoeffnet.",
    ];
    for (i, text) in steps.iter().enumerate() {
        layer.use_text(text, 10.0, Mm(20.0), Mm(160.0 - (i as f64 * 8.0)), &font);
    }

    layer.use_text(
        "SD-Vault — Zero-Knowledge Passwort-Manager",
        9.0, Mm(20.0), Mm(15.0), &font,
    );

    let mut buffer = Vec::new();
    doc.save(&mut BufWriter::new(&mut buffer))
        .map_err(|e| e.to_string())?;

    Ok(buffer)
}
```

- [ ] **Step 2: `src-tauri/src/lib.rs` aktualisieren**

```rust
pub mod crypto;
pub mod db;
pub mod emergency_kit;
```

- [ ] **Step 3: Kompilierung prüfen**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/emergency_kit.rs src-tauri/src/lib.rs
git commit -m "feat: add emergency kit PDF generation"
```

---

## Task 5: Sync Infrastructure

**Files:**
- Create: `src-tauri/src/sync/mod.rs`
- Create: `src-tauri/src/sync/config.rs`
- Create: `src-tauri/src/sync/local.rs`
- Create: `src-tauri/src/sync/google_drive.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: `src-tauri/src/sync/mod.rs` erstellen**

```rust
use async_trait::async_trait;
use serde::{Deserialize, Serialize};

pub mod config;
pub mod google_drive;
pub mod local;
pub mod sftp;

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct BackupEntry {
    pub id: String,
    pub timestamp: i64,
    pub size_bytes: u64,
}

#[async_trait]
pub trait SyncProvider: Send + Sync {
    async fn upload(&self, data: &[u8], name: &str) -> Result<(), String>;
    async fn download(&self, id: &str) -> Result<Vec<u8>, String>;
    async fn list_backups(&self) -> Result<Vec<BackupEntry>, String>;
    async fn delete_backup(&self, id: &str) -> Result<(), String>;
}
```

- [ ] **Step 2: `src-tauri/src/sync/config.rs` erstellen**

```rust
use serde::{Deserialize, Serialize};
use std::path::Path;

#[derive(Debug, Serialize, Deserialize, Clone)]
#[serde(tag = "type")]
pub enum SftpAuth {
    Password { password: String },
    KeyFile { key_path: String },
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct SftpConfig {
    pub host: String,
    pub port: u16,
    pub username: String,
    pub remote_path: String,
    pub auth: SftpAuth,
}

#[derive(Debug, Serialize, Deserialize, Default)]
pub struct SyncConfig {
    pub sftp: Option<SftpConfig>,
}

impl SyncConfig {
    pub fn load(config_path: &Path) -> Result<Self, String> {
        if !config_path.exists() {
            return Ok(Self::default());
        }
        let content = std::fs::read_to_string(config_path).map_err(|e| e.to_string())?;
        toml::from_str(&content).map_err(|e| e.to_string())
    }

    pub fn save(&self, config_path: &Path) -> Result<(), String> {
        let content = toml::to_string_pretty(self).map_err(|e| e.to_string())?;
        std::fs::write(config_path, content).map_err(|e| e.to_string())
    }
}
```

- [ ] **Step 3: `src-tauri/src/sync/local.rs` erstellen**

```rust
use async_trait::async_trait;
use chrono::Utc;
use std::fs;
use std::path::{Path, PathBuf};

use super::{BackupEntry, SyncProvider};

pub struct LocalBackupProvider {
    backup_dir: PathBuf,
    max_backups: usize,
}

impl LocalBackupProvider {
    pub fn new(vault_dir: &Path) -> Result<Self, String> {
        let backup_dir = vault_dir.join("backups");
        fs::create_dir_all(&backup_dir).map_err(|e| e.to_string())?;
        Ok(Self { backup_dir, max_backups: 10 })
    }

    fn prune(&self) -> Result<(), String> {
        let mut entries: Vec<(i64, PathBuf)> = fs::read_dir(&self.backup_dir)
            .map_err(|e| e.to_string())?
            .filter_map(|e| e.ok())
            .filter(|e| e.path().extension().map(|x| x == "db").unwrap_or(false))
            .filter_map(|e| {
                let name = e.file_name().to_string_lossy().to_string();
                let ts: i64 = name.strip_prefix("vault_")?.strip_suffix(".db")?.parse().ok()?;
                Some((ts, e.path()))
            })
            .collect();

        entries.sort_by(|a, b| b.0.cmp(&a.0));
        for (_, path) in entries.into_iter().skip(self.max_backups) {
            let _ = fs::remove_file(path);
        }
        Ok(())
    }
}

#[async_trait]
impl SyncProvider for LocalBackupProvider {
    async fn upload(&self, data: &[u8], _name: &str) -> Result<(), String> {
        let filename = format!("vault_{}.db", Utc::now().timestamp());
        fs::write(self.backup_dir.join(&filename), data).map_err(|e| e.to_string())?;
        self.prune()
    }

    async fn download(&self, id: &str) -> Result<Vec<u8>, String> {
        fs::read(self.backup_dir.join(id)).map_err(|e| e.to_string())
    }

    async fn list_backups(&self) -> Result<Vec<BackupEntry>, String> {
        let mut entries: Vec<BackupEntry> = fs::read_dir(&self.backup_dir)
            .map_err(|e| e.to_string())?
            .filter_map(|e| e.ok())
            .filter(|e| e.path().extension().map(|x| x == "db").unwrap_or(false))
            .filter_map(|e| {
                let name = e.file_name().to_string_lossy().to_string();
                let size = e.metadata().ok()?.len();
                let ts: i64 = name.strip_prefix("vault_")?.strip_suffix(".db")?.parse().ok()?;
                Some(BackupEntry { id: name, timestamp: ts, size_bytes: size })
            })
            .collect();
        entries.sort_by(|a, b| b.timestamp.cmp(&a.timestamp));
        Ok(entries)
    }

    async fn delete_backup(&self, id: &str) -> Result<(), String> {
        fs::remove_file(self.backup_dir.join(id)).map_err(|e| e.to_string())
    }
}
```

- [ ] **Step 4: `src-tauri/src/sync/google_drive.rs` erstellen**

```rust
use async_trait::async_trait;
use super::{BackupEntry, SyncProvider};

/// Google Drive Sync — Stub. OAuth2-Implementierung folgt in einem späteren Release.
pub struct GoogleDriveProvider;

#[async_trait]
impl SyncProvider for GoogleDriveProvider {
    async fn upload(&self, _data: &[u8], _name: &str) -> Result<(), String> {
        Err("Google Drive Sync noch nicht implementiert".into())
    }
    async fn download(&self, _id: &str) -> Result<Vec<u8>, String> {
        Err("Google Drive Sync noch nicht implementiert".into())
    }
    async fn list_backups(&self) -> Result<Vec<BackupEntry>, String> {
        Err("Google Drive Sync noch nicht implementiert".into())
    }
    async fn delete_backup(&self, _id: &str) -> Result<(), String> {
        Err("Google Drive Sync noch nicht implementiert".into())
    }
}
```

- [ ] **Step 5: `src-tauri/src/lib.rs` aktualisieren**

```rust
pub mod crypto;
pub mod db;
pub mod emergency_kit;
pub mod sync;
```

- [ ] **Step 6: Kompilierung prüfen**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/sync/ src-tauri/src/lib.rs
git commit -m "feat: add sync infrastructure (trait + local backup + Google Drive stub)"
```

---

## Task 6: SFTP Sync Provider

**Files:**
- Create: `src-tauri/src/sync/sftp.rs`

- [ ] **Step 1: `src-tauri/src/sync/sftp.rs` erstellen**

```rust
use async_trait::async_trait;
use chrono::Utc;
use ssh2::Session;
use std::io::{Read, Write};
use std::net::TcpStream;
use std::path::Path;

use super::config::{SftpAuth, SftpConfig};
use super::{BackupEntry, SyncProvider};

pub struct SftpProvider {
    config: SftpConfig,
}

impl SftpProvider {
    pub fn new(config: SftpConfig) -> Self {
        Self { config }
    }

    fn connect(&self) -> Result<Session, String> {
        let addr = format!("{}:{}", self.config.host, self.config.port);
        let tcp = TcpStream::connect(&addr)
            .map_err(|e| format!("SFTP Verbindung fehlgeschlagen: {}", e))?;

        let mut sess = Session::new().map_err(|e| e.to_string())?;
        sess.set_tcp_stream(tcp);
        sess.handshake()
            .map_err(|e| format!("SSH Handshake fehlgeschlagen: {}", e))?;

        match &self.config.auth {
            SftpAuth::Password { password } => {
                sess.userauth_password(&self.config.username, password)
                    .map_err(|e| format!("SFTP Passwort-Auth fehlgeschlagen: {}", e))?;
            }
            SftpAuth::KeyFile { key_path } => {
                sess.userauth_pubkey_file(&self.config.username, None, Path::new(key_path), None)
                    .map_err(|e| format!("SFTP Key-Auth fehlgeschlagen: {}", e))?;
            }
        }

        if !sess.authenticated() {
            return Err("SFTP Authentifizierung fehlgeschlagen".into());
        }
        Ok(sess)
    }

    fn remote_path(&self, filename: &str) -> String {
        format!("{}/{}", self.config.remote_path.trim_end_matches('/'), filename)
    }
}

#[async_trait]
impl SyncProvider for SftpProvider {
    async fn upload(&self, data: &[u8], _name: &str) -> Result<(), String> {
        let sess = self.connect()?;
        let sftp = sess.sftp().map_err(|e| e.to_string())?;

        let _ = sftp.mkdir(Path::new(&self.config.remote_path), 0o755);

        let filename = format!("vault_{}.db", Utc::now().timestamp());
        let remote = self.remote_path(&filename);

        let mut file = sftp
            .create(Path::new(&remote))
            .map_err(|e| format!("SFTP Datei erstellen fehlgeschlagen: {}", e))?;
        file.write_all(data)
            .map_err(|e| format!("SFTP Schreiben fehlgeschlagen: {}", e))?;

        Ok(())
    }

    async fn download(&self, id: &str) -> Result<Vec<u8>, String> {
        let sess = self.connect()?;
        let sftp = sess.sftp().map_err(|e| e.to_string())?;

        let remote = self.remote_path(id);
        let mut file = sftp
            .open(Path::new(&remote))
            .map_err(|e| format!("SFTP Öffnen fehlgeschlagen: {}", e))?;

        let mut data = Vec::new();
        file.read_to_end(&mut data)
            .map_err(|e| format!("SFTP Lesen fehlgeschlagen: {}", e))?;
        Ok(data)
    }

    async fn list_backups(&self) -> Result<Vec<BackupEntry>, String> {
        let sess = self.connect()?;
        let sftp = sess.sftp().map_err(|e| e.to_string())?;

        let dir_entries = sftp
            .readdir(Path::new(&self.config.remote_path))
            .map_err(|e| format!("SFTP readdir fehlgeschlagen: {}", e))?;

        let mut backups: Vec<BackupEntry> = dir_entries
            .into_iter()
            .filter(|(path, _)| path.extension().map(|x| x == "db").unwrap_or(false))
            .filter_map(|(path, stat)| {
                let name = path.file_name()?.to_string_lossy().to_string();
                let ts: i64 = name.strip_prefix("vault_")?.strip_suffix(".db")?.parse().ok()?;
                Some(BackupEntry {
                    id: name,
                    timestamp: ts,
                    size_bytes: stat.size.unwrap_or(0),
                })
            })
            .collect();

        backups.sort_by(|a, b| b.timestamp.cmp(&a.timestamp));
        Ok(backups)
    }

    async fn delete_backup(&self, id: &str) -> Result<(), String> {
        let sess = self.connect()?;
        let sftp = sess.sftp().map_err(|e| e.to_string())?;
        sftp.unlink(Path::new(&self.remote_path(id)))
            .map_err(|e| format!("SFTP Löschen fehlgeschlagen: {}", e))
    }
}
```

- [ ] **Step 2: Kompilierung prüfen**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

- [ ] **Step 3: Commit**

```bash
git add src-tauri/src/sync/sftp.rs
git commit -m "feat: add SFTP sync provider (ssh2)"
```

---

## Task 7: Tauri Commands (AppState + IPC)

**Files:**
- Create: `src-tauri/src/main.rs`
- Replace: `src-tauri/src/lib.rs`

- [ ] **Step 1: `src-tauri/src/main.rs` erstellen**

```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    sd_vault_lib::run();
}
```

- [ ] **Step 2: `src-tauri/src/lib.rs` vollständig ersetzen**

```rust
pub mod crypto;
pub mod db;
pub mod emergency_kit;
pub mod sync;

use chrono::Utc;
use db::{ItemPayload, ItemWithPayload, VaultMeta};
use dirs::document_dir;
use rusqlite::Connection;
use std::fs;
use std::path::PathBuf;
use std::sync::Mutex;
use tauri::State;
use zeroize::Zeroizing;

use sync::config::{SftpConfig, SyncConfig};
use sync::local::LocalBackupProvider;
use sync::sftp::SftpProvider;
use sync::{BackupEntry, SyncProvider};

pub struct AppState {
    pub master_key: Mutex<Option<Zeroizing<[u8; 32]>>>,
    pub db_conn: Mutex<Option<Connection>>,
    pub vault_dir: Mutex<Option<PathBuf>>,
}

impl Default for AppState {
    fn default() -> Self {
        Self {
            master_key: Mutex::new(None),
            db_conn: Mutex::new(None),
            vault_dir: Mutex::new(None),
        }
    }
}

fn get_vault_dir() -> Result<PathBuf, String> {
    let docs = document_dir().ok_or("Dokumente-Ordner nicht gefunden")?;
    let dir = docs.join("SD-Vault");
    fs::create_dir_all(&dir).map_err(|e| e.to_string())?;
    Ok(dir)
}

fn db_path() -> Result<PathBuf, String> {
    Ok(get_vault_dir()?.join("vault.db"))
}

fn salt_path() -> Result<PathBuf, String> {
    Ok(get_vault_dir()?.join("vault.salt"))
}

fn write_salt(salt: &[u8; 32]) -> Result<(), String> {
    fs::write(salt_path()?, salt.as_ref()).map_err(|e| e.to_string())
}

fn read_salt() -> Result<[u8; 32], String> {
    let bytes = fs::read(salt_path()?).map_err(|e| format!("vault.salt nicht gefunden: {}", e))?;
    if bytes.len() != 32 {
        return Err("Korrupte vault.salt Datei".into());
    }
    let mut salt = [0u8; 32];
    salt.copy_from_slice(&bytes);
    Ok(salt)
}

#[derive(serde::Serialize)]
pub struct CreateVaultResult {
    pub vault_id: String,
    pub secret_key_formatted: String,
    pub created_at_formatted: String,
    pub pdf_bytes: Vec<u8>,
}

/// Prüft ob ein Vault auf Disk existiert.
#[tauri::command]
pub fn vault_exists() -> bool {
    db_path().map(|p| p.exists()).unwrap_or(false)
}

/// Erstellt einen neuen Vault. Gibt Secret Key + PDF-Bytes zurück.
/// Frontend MUSS das PDF anzeigen/speichern bevor der Vault als erstellt gilt.
#[tauri::command]
pub fn create_vault(
    state: State<'_, AppState>,
    master_pw: String,
) -> Result<CreateVaultResult, String> {
    let db_path = db_path()?;
    if db_path.exists() {
        return Err("Vault existiert bereits".into());
    }

    let (secret_key_bytes, secret_key_formatted) = crypto::generate_secret_key();
    let salt = crypto::generate_salt();
    write_salt(&salt)?;

    let master_key = crypto::derive_master_key(&master_pw, &secret_key_bytes, &salt)?;
    let sqlcipher_key = crypto::derive_sqlcipher_key(&master_key);
    let conn = db::open_db(&db_path, &sqlcipher_key)?;

    let vault_id = uuid::Uuid::new_v4().to_string();
    db::init_vault_meta(&conn, &vault_id, &salt)?;

    let now = Utc::now();
    let created_at_formatted = now.format("%d. %B %Y").to_string();
    let pdf_bytes =
        emergency_kit::generate_pdf(&vault_id, &secret_key_formatted, &created_at_formatted)?;

    *state.master_key.lock().unwrap() = Some(master_key);
    *state.db_conn.lock().unwrap() = Some(conn);
    *state.vault_dir.lock().unwrap() = Some(get_vault_dir()?);

    Ok(CreateVaultResult {
        vault_id,
        secret_key_formatted,
        created_at_formatted,
        pdf_bytes,
    })
}

/// Entsperrt den Vault mit Master-Passwort + Secret Key.
#[tauri::command]
pub fn unlock_vault(
    state: State<'_, AppState>,
    master_pw: String,
    secret_key_formatted: String,
) -> Result<VaultMeta, String> {
    let db_path = db_path()?;
    if !db_path.exists() {
        return Err("Kein Vault gefunden. Bitte zuerst einen neuen Vault erstellen.".into());
    }

    let secret_key_bytes = crypto::parse_secret_key(&secret_key_formatted)?;
    let salt = read_salt()?;
    let master_key = crypto::derive_master_key(&master_pw, &secret_key_bytes, &salt)?;

    let sqlcipher_key = crypto::derive_sqlcipher_key(&master_key);
    let conn = db::open_db(&db_path, &sqlcipher_key)?;
    let meta = db::read_vault_meta(&conn)?;

    *state.master_key.lock().unwrap() = Some(master_key);
    *state.db_conn.lock().unwrap() = Some(conn);
    *state.vault_dir.lock().unwrap() = Some(get_vault_dir()?);

    Ok(meta)
}

/// Sperrt den Vault — zeroized Master Key, schließt DB.
#[tauri::command]
pub fn lock_vault(state: State<'_, AppState>) {
    if let Ok(mut k) = state.master_key.lock() {
        k.take(); // Zeroizing<> zeroed on drop
    }
    if let Ok(mut c) = state.db_conn.lock() {
        c.take();
    }
}

/// Gibt alle Einträge zurück (optional nach Kategorie gefiltert), entschlüsselt.
#[tauri::command]
pub fn get_items(
    state: State<'_, AppState>,
    category: Option<String>,
) -> Result<Vec<ItemWithPayload>, String> {
    let key_guard = state.master_key.lock().unwrap();
    let master_key = key_guard.as_ref().ok_or("Vault ist gesperrt")?;
    let entry_key = crypto::derive_entry_key(master_key);

    let conn_guard = state.db_conn.lock().unwrap();
    let conn = conn_guard.as_ref().ok_or("Vault ist gesperrt")?;

    db::get_items(conn, &entry_key, category.as_deref())
}

/// Fügt einen neuen Eintrag hinzu.
#[tauri::command]
pub fn add_item(
    state: State<'_, AppState>,
    category: String,
    payload: ItemPayload,
) -> Result<String, String> {
    let key_guard = state.master_key.lock().unwrap();
    let master_key = key_guard.as_ref().ok_or("Vault ist gesperrt")?;
    let entry_key = crypto::derive_entry_key(master_key);

    let conn_guard = state.db_conn.lock().unwrap();
    let conn = conn_guard.as_ref().ok_or("Vault ist gesperrt")?;

    db::insert_item(conn, &entry_key, &category, &payload)
}

/// Aktualisiert einen bestehenden Eintrag.
#[tauri::command]
pub fn update_item(
    state: State<'_, AppState>,
    id: String,
    payload: ItemPayload,
) -> Result<(), String> {
    let key_guard = state.master_key.lock().unwrap();
    let master_key = key_guard.as_ref().ok_or("Vault ist gesperrt")?;
    let entry_key = crypto::derive_entry_key(master_key);

    let conn_guard = state.db_conn.lock().unwrap();
    let conn = conn_guard.as_ref().ok_or("Vault ist gesperrt")?;

    db::update_item(conn, &entry_key, &id, &payload)
}

/// Löscht einen Eintrag nach ID.
#[tauri::command]
pub fn delete_item(state: State<'_, AppState>, id: String) -> Result<(), String> {
    let conn_guard = state.db_conn.lock().unwrap();
    let conn = conn_guard.as_ref().ok_or("Vault ist gesperrt")?;
    db::delete_item(conn, &id)
}

/// Lokales Backup erstellen.
#[tauri::command]
pub async fn sync_local(state: State<'_, AppState>) -> Result<(), String> {
    let vault_dir = state.vault_dir.lock().unwrap().clone().ok_or("Vault ist gesperrt")?;
    let data = fs::read(vault_dir.join("vault.db")).map_err(|e| e.to_string())?;
    LocalBackupProvider::new(&vault_dir)?.upload(&data, "vault.db").await
}

/// SFTP-Sync mit gespeicherter Konfiguration.
#[tauri::command]
pub async fn sync_sftp(state: State<'_, AppState>) -> Result<(), String> {
    let vault_dir = state.vault_dir.lock().unwrap().clone().ok_or("Vault ist gesperrt")?;
    let config = SyncConfig::load(&vault_dir.join("sync_config.toml"))?;
    let sftp_config = config.sftp.ok_or("Keine SFTP-Konfiguration gefunden")?;

    let data = fs::read(vault_dir.join("vault.db")).map_err(|e| e.to_string())?;
    SftpProvider::new(sftp_config).upload(&data, "vault.db").await
}

/// Speichert SFTP-Konfiguration.
#[tauri::command]
pub fn save_sftp_config(
    state: State<'_, AppState>,
    sftp_config: SftpConfig,
) -> Result<(), String> {
    let vault_dir = state.vault_dir.lock().unwrap().clone().ok_or("Vault ist gesperrt")?;
    let config_path = vault_dir.join("sync_config.toml");
    let mut config = SyncConfig::load(&config_path).unwrap_or_default();
    config.sftp = Some(sftp_config);
    config.save(&config_path)
}

/// Listet lokale Backups auf.
#[tauri::command]
pub async fn list_local_backups(state: State<'_, AppState>) -> Result<Vec<BackupEntry>, String> {
    let vault_dir = state.vault_dir.lock().unwrap().clone().ok_or("Vault ist gesperrt")?;
    LocalBackupProvider::new(&vault_dir)?.list_backups().await
}

pub fn run() {
    tauri::Builder::default()
        .manage(AppState::default())
        .plugin(tauri_plugin_dialog::init())
        .invoke_handler(tauri::generate_handler![
            vault_exists,
            create_vault,
            unlock_vault,
            lock_vault,
            get_items,
            add_item,
            update_item,
            delete_item,
            sync_local,
            sync_sftp,
            save_sftp_config,
            list_local_backups,
        ])
        .run(tauri::generate_context!())
        .expect("SD-Vault konnte nicht gestartet werden");
}
```

- [ ] **Step 3: Kompilierung prüfen**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

Expected: Kompiliert ohne Fehler

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/main.rs src-tauri/src/lib.rs
git commit -m "feat: add Tauri AppState and all IPC commands"
```

---

## Task 8: Frontend — Types & Utils

**Files:**
- Create: `src/types.ts`
- Create: `src/utils/clipboard.ts`
- Create: `src/utils/strength.ts`
- Create: `src/main.tsx`
- Create: `src/App.css`

- [ ] **Step 1: `src/types.ts` erstellen**

```typescript
export interface VaultMeta {
    id: string;
    created_at: number;
    argon2_mem: number;
    argon2_ops: number;
}

export interface CustomField {
    label: string;
    value: string;
}

export interface ItemPayload {
    title: string;
    username: string;
    password: string;
    url: string;
    notes: string;
    fields: CustomField[];
}

export interface ItemWithPayload {
    id: string;
    category: ItemCategory;
    updated_at: number;
    payload: ItemPayload;
}

export type ItemCategory = 'login' | 'card' | 'note' | 'identity';

export interface BackupEntry {
    id: string;
    timestamp: number;
    size_bytes: number;
}

export interface CreateVaultResult {
    vault_id: string;
    secret_key_formatted: string;
    created_at_formatted: string;
    pdf_bytes: number[];
}

export type AppScreen =
    | 'loading'
    | 'lock'
    | 'first-run'
    | 'vault'
    | 'settings';
```

- [ ] **Step 2: `src/utils/clipboard.ts` erstellen**

```typescript
let clearTimer: ReturnType<typeof setTimeout> | null = null;

/** Kopiert text in die Zwischenablage und löscht ihn automatisch nach `ms` Millisekunden. */
export async function copyWithAutoClear(text: string, ms = 30_000): Promise<void> {
    await navigator.clipboard.writeText(text);

    if (clearTimer !== null) {
        clearTimeout(clearTimer);
    }
    clearTimer = setTimeout(() => {
        navigator.clipboard.writeText('').catch(() => {});
        clearTimer = null;
    }, ms);
}
```

- [ ] **Step 3: `src/utils/strength.ts` erstellen**

```typescript
import zxcvbn from 'zxcvbn';

export interface StrengthResult {
    score: 0 | 1 | 2 | 3 | 4;
    label: string;
    color: string;
}

const LABELS = ['Sehr schwach', 'Schwach', 'Mittel', 'Stark', 'Sehr stark'];
const COLORS = [
    'bg-red-600',
    'bg-orange-500',
    'bg-yellow-500',
    'bg-green-500',
    'bg-emerald-500',
];

export function checkStrength(password: string): StrengthResult {
    const result = zxcvbn(password);
    return {
        score: result.score,
        label: LABELS[result.score],
        color: COLORS[result.score],
    };
}
```

- [ ] **Step 4: `src/main.tsx` erstellen**

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './App.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
```

- [ ] **Step 5: `src/App.css` erstellen**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    background-color: #0f1117;
    color: #e5e7eb;
    font-family: system-ui, -apple-system, sans-serif;
    -webkit-font-smoothing: antialiased;
    user-select: none;
}

input, textarea {
    user-select: text;
}
```

- [ ] **Step 6: Commit**

```bash
git add src/types.ts src/utils/ src/main.tsx src/App.css
git commit -m "feat: add frontend types and utilities"
```

---

## Task 9: LockScreen & FirstRunSetup

**Files:**
- Create: `src/components/LockScreen.tsx`
- Create: `src/components/FirstRunSetup.tsx`

- [ ] **Step 1: `src/components/LockScreen.tsx` erstellen**

```tsx
import { useState } from 'react';
import { invoke } from '@tauri-apps/api/core';
import type { VaultMeta } from '../types';

interface Props {
    onUnlocked: (meta: VaultMeta) => void;
    onCreateNew: () => void;
    vaultExists: boolean;
}

export default function LockScreen({ onUnlocked, onCreateNew, vaultExists }: Props) {
    const [masterPw, setMasterPw] = useState('');
    const [secretKey, setSecretKey] = useState('');
    const [error, setError] = useState('');
    const [loading, setLoading] = useState(false);

    async function handleUnlock(e: React.FormEvent) {
        e.preventDefault();
        setError('');
        setLoading(true);
        try {
            const meta = await invoke<VaultMeta>('unlock_vault', {
                masterPw,
                secretKeyFormatted: secretKey,
            });
            onUnlocked(meta);
        } catch (err) {
            setError(String(err));
        } finally {
            setLoading(false);
        }
    }

    return (
        <div className="min-h-screen bg-vault-bg flex items-center justify-center">
            <div className="w-full max-w-md">
                <div className="text-center mb-8">
                    <div className="text-4xl mb-3">🔐</div>
                    <h1 className="text-2xl font-bold text-white">SD-Vault</h1>
                    <p className="text-vault-muted text-sm mt-1">Sicher. Lokal. Zero-Knowledge.</p>
                </div>

                {vaultExists ? (
                    <form onSubmit={handleUnlock} className="bg-vault-surface border border-vault-border rounded-xl p-6 space-y-4">
                        <div>
                            <label className="block text-sm text-vault-muted mb-1">Master-Passwort</label>
                            <input
                                type="password"
                                value={masterPw}
                                onChange={e => setMasterPw(e.target.value)}
                                className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-white focus:outline-none focus:border-vault-accent"
                                placeholder="Master-Passwort eingeben"
                                required
                                autoFocus
                            />
                        </div>

                        <div>
                            <label className="block text-sm text-vault-muted mb-1">Secret Key</label>
                            <input
                                type="text"
                                value={secretKey}
                                onChange={e => setSecretKey(e.target.value.toUpperCase())}
                                className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-white font-mono focus:outline-none focus:border-vault-accent"
                                placeholder="SDVLT-XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX"
                                required
                            />
                            <p className="text-xs text-vault-muted mt-1">
                                Den Secret Key findest du in deinem Emergency Kit PDF.
                            </p>
                        </div>

                        {error && (
                            <div className="bg-red-900/30 border border-vault-danger rounded-lg px-3 py-2 text-sm text-red-400">
                                {error}
                            </div>
                        )}

                        <button
                            type="submit"
                            disabled={loading}
                            className="w-full bg-vault-accent hover:bg-indigo-500 disabled:opacity-50 text-white font-medium rounded-lg py-2.5 transition-colors"
                        >
                            {loading ? 'Wird entsperrt…' : 'Vault öffnen'}
                        </button>
                    </form>
                ) : (
                    <div className="bg-vault-surface border border-vault-border rounded-xl p-6 text-center space-y-4">
                        <p className="text-vault-muted">Kein Vault gefunden.</p>
                        <button
                            onClick={onCreateNew}
                            className="w-full bg-vault-accent hover:bg-indigo-500 text-white font-medium rounded-lg py-2.5 transition-colors"
                        >
                            Neuen Vault erstellen
                        </button>
                    </div>
                )}
            </div>
        </div>
    );
}
```

- [ ] **Step 2: `src/components/FirstRunSetup.tsx` erstellen**

```tsx
import { useState } from 'react';
import { invoke } from '@tauri-apps/api/core';
import { save } from '@tauri-apps/plugin-dialog';
import type { CreateVaultResult, VaultMeta } from '../types';
import { checkStrength } from '../utils/strength';

interface Props {
    onVaultCreated: (meta: VaultMeta) => void;
}

type Step = 'password' | 'kit' | 'confirm';

export default function FirstRunSetup({ onVaultCreated }: Props) {
    const [step, setStep] = useState<Step>('password');
    const [masterPw, setMasterPw] = useState('');
    const [masterPwConfirm, setMasterPwConfirm] = useState('');
    const [createResult, setCreateResult] = useState<CreateVaultResult | null>(null);
    const [pdfSaved, setPdfSaved] = useState(false);
    const [confirmed, setConfirmed] = useState(false);
    const [error, setError] = useState('');
    const [loading, setLoading] = useState(false);

    const strength = checkStrength(masterPw);

    async function handleCreateVault(e: React.FormEvent) {
        e.preventDefault();
        if (masterPw !== masterPwConfirm) {
            setError('Passwörter stimmen nicht überein.');
            return;
        }
        if (strength.score < 2) {
            setError('Passwort ist zu schwach. Bitte ein stärkeres Passwort wählen.');
            return;
        }
        setError('');
        setLoading(true);
        try {
            const result = await invoke<CreateVaultResult>('create_vault', { masterPw });
            setCreateResult(result);
            setStep('kit');
        } catch (err) {
            setError(String(err));
        } finally {
            setLoading(false);
        }
    }

    async function handleSavePdf() {
        if (!createResult) return;
        const filePath = await save({
            defaultPath: 'SD-Vault-Emergency-Kit.pdf',
            filters: [{ name: 'PDF', extensions: ['pdf'] }],
        });
        if (!filePath) return;

        const bytes = new Uint8Array(createResult.pdf_bytes);
        // Datei via Tauri FS schreiben — wir nutzen den writeBinaryFile Command
        await invoke('plugin:fs|write_file', {
            path: filePath,
            contents: Array.from(bytes),
        });
        setPdfSaved(true);
    }

    function handleFinish() {
        if (!createResult || !confirmed) return;
        // Vault ist bereits in AppState — wir simulieren ein VaultMeta
        const meta: VaultMeta = {
            id: createResult.vault_id,
            created_at: Date.now() / 1000,
            argon2_mem: 262144,
            argon2_ops: 3,
        };
        onVaultCreated(meta);
    }

    return (
        <div className="min-h-screen bg-vault-bg flex items-center justify-center">
            <div className="w-full max-w-lg">
                <div className="text-center mb-8">
                    <div className="text-4xl mb-3">🔐</div>
                    <h1 className="text-2xl font-bold text-white">Neuen Vault erstellen</h1>
                </div>

                {/* Step indicator */}
                <div className="flex items-center justify-center gap-2 mb-6">
                    {(['password', 'kit', 'confirm'] as Step[]).map((s, i) => (
                        <div key={s} className="flex items-center gap-2">
                            <div className={`w-7 h-7 rounded-full flex items-center justify-center text-xs font-bold
                                ${step === s ? 'bg-vault-accent text-white' :
                                  (step === 'kit' && i === 0) || (step === 'confirm' && i <= 1) ? 'bg-green-600 text-white' :
                                  'bg-vault-border text-vault-muted'}`}>
                                {(step === 'kit' && i === 0) || (step === 'confirm' && i <= 1) ? '✓' : i + 1}
                            </div>
                            {i < 2 && <div className="w-8 h-px bg-vault-border" />}
                        </div>
                    ))}
                </div>

                <div className="bg-vault-surface border border-vault-border rounded-xl p-6">
                    {step === 'password' && (
                        <form onSubmit={handleCreateVault} className="space-y-4">
                            <h2 className="font-semibold text-white">Master-Passwort wählen</h2>
                            <p className="text-sm text-vault-muted">
                                Das Master-Passwort schützt zusammen mit dem Secret Key deinen Vault.
                                Wähle ein starkes, einzigartiges Passwort.
                            </p>

                            <div>
                                <label className="block text-sm text-vault-muted mb-1">Master-Passwort</label>
                                <input
                                    type="password"
                                    value={masterPw}
                                    onChange={e => setMasterPw(e.target.value)}
                                    className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-white focus:outline-none focus:border-vault-accent"
                                    required autoFocus
                                />
                                {masterPw && (
                                    <div className="mt-2">
                                        <div className="flex gap-1 mb-1">
                                            {[0, 1, 2, 3, 4].map(i => (
                                                <div key={i} className={`h-1 flex-1 rounded-full ${i <= strength.score ? strength.color : 'bg-vault-border'}`} />
                                            ))}
                                        </div>
                                        <p className="text-xs text-vault-muted">{strength.label}</p>
                                    </div>
                                )}
                            </div>

                            <div>
                                <label className="block text-sm text-vault-muted mb-1">Passwort bestätigen</label>
                                <input
                                    type="password"
                                    value={masterPwConfirm}
                                    onChange={e => setMasterPwConfirm(e.target.value)}
                                    className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-white focus:outline-none focus:border-vault-accent"
                                    required
                                />
                            </div>

                            {error && (
                                <div className="bg-red-900/30 border border-vault-danger rounded-lg px-3 py-2 text-sm text-red-400">
                                    {error}
                                </div>
                            )}

                            <button
                                type="submit"
                                disabled={loading}
                                className="w-full bg-vault-accent hover:bg-indigo-500 disabled:opacity-50 text-white font-medium rounded-lg py-2.5 transition-colors"
                            >
                                {loading ? 'Vault wird erstellt…' : 'Weiter'}
                            </button>
                        </form>
                    )}

                    {step === 'kit' && createResult && (
                        <div className="space-y-4">
                            <h2 className="font-semibold text-white">Emergency Kit speichern</h2>
                            <div className="bg-amber-900/30 border border-amber-600/50 rounded-lg p-3">
                                <p className="text-sm text-amber-300 font-medium">⚠️ Wichtig</p>
                                <p className="text-sm text-amber-200 mt-1">
                                    Dein Secret Key ist einmalig generiert und kann nicht wiederhergestellt werden.
                                    Speichere das Emergency Kit PDF sicher (z.B. gedruckt in einem Safe).
                                </p>
                            </div>

                            <div className="bg-vault-bg border border-vault-border rounded-lg p-4 font-mono text-sm text-center">
                                <p className="text-vault-muted text-xs mb-1">Secret Key</p>
                                <p className="text-white tracking-wider">{createResult.secret_key_formatted}</p>
                            </div>

                            <button
                                onClick={handleSavePdf}
                                className={`w-full border rounded-lg py-2.5 font-medium transition-colors ${
                                    pdfSaved
                                        ? 'bg-green-900/30 border-green-600 text-green-400'
                                        : 'bg-vault-accent hover:bg-indigo-500 border-transparent text-white'
                                }`}
                            >
                                {pdfSaved ? '✓ PDF gespeichert' : 'Emergency Kit PDF speichern'}
                            </button>

                            {pdfSaved && (
                                <button
                                    onClick={() => setStep('confirm')}
                                    className="w-full bg-vault-surface border border-vault-border hover:border-vault-accent text-white font-medium rounded-lg py-2.5 transition-colors"
                                >
                                    Weiter
                                </button>
                            )}
                        </div>
                    )}

                    {step === 'confirm' && (
                        <div className="space-y-4">
                            <h2 className="font-semibold text-white">Bestätigung</h2>

                            <label className="flex items-start gap-3 cursor-pointer">
                                <input
                                    type="checkbox"
                                    checked={confirmed}
                                    onChange={e => setConfirmed(e.target.checked)}
                                    className="mt-0.5 accent-indigo-500"
                                />
                                <span className="text-sm text-vault-muted">
                                    Ich habe das Emergency Kit PDF sicher gespeichert. Mir ist bewusst,
                                    dass ohne dieses PDF und mein Master-Passwort der Vault
                                    <strong className="text-white"> permanent verloren</strong> ist.
                                </span>
                            </label>

                            <button
                                onClick={handleFinish}
                                disabled={!confirmed}
                                className="w-full bg-vault-accent hover:bg-indigo-500 disabled:opacity-40 disabled:cursor-not-allowed text-white font-medium rounded-lg py-2.5 transition-colors"
                            >
                                Vault öffnen
                            </button>
                        </div>
                    )}
                </div>
            </div>
        </div>
    );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/LockScreen.tsx src/components/FirstRunSetup.tsx
git commit -m "feat: add LockScreen and FirstRunSetup components"
```

---

## Task 10: VaultView — Sidebar, EntryList, EntryDetail

**Files:**
- Create: `src/components/Sidebar.tsx`
- Create: `src/components/EntryList.tsx`
- Create: `src/components/EntryDetail.tsx`
- Create: `src/components/VaultView.tsx`

- [ ] **Step 1: `src/components/Sidebar.tsx` erstellen**

```tsx
import type { ItemCategory } from '../types';

interface Props {
    activeCategory: ItemCategory | 'all';
    onCategoryChange: (cat: ItemCategory | 'all') => void;
    searchQuery: string;
    onSearchChange: (q: string) => void;
    onSettings: () => void;
    onLock: () => void;
}

const CATEGORIES: { value: ItemCategory | 'all'; label: string; icon: string }[] = [
    { value: 'all', label: 'Alle Einträge', icon: '🗂️' },
    { value: 'login', label: 'Logins', icon: '🔑' },
    { value: 'card', label: 'Karten', icon: '💳' },
    { value: 'note', label: 'Notizen', icon: '📝' },
    { value: 'identity', label: 'Identitäten', icon: '👤' },
];

export default function Sidebar({ activeCategory, onCategoryChange, searchQuery, onSearchChange, onSettings, onLock }: Props) {
    return (
        <aside className="w-56 bg-vault-surface border-r border-vault-border flex flex-col h-screen">
            <div className="p-4 border-b border-vault-border">
                <div className="flex items-center gap-2 mb-3">
                    <span className="text-lg">🔐</span>
                    <span className="font-bold text-white text-sm">SD-Vault</span>
                </div>
                <input
                    type="text"
                    value={searchQuery}
                    onChange={e => onSearchChange(e.target.value)}
                    placeholder="Suchen…"
                    className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-1.5 text-sm text-white focus:outline-none focus:border-vault-accent placeholder:text-vault-muted"
                />
            </div>

            <nav className="flex-1 p-2 space-y-0.5 overflow-y-auto">
                {CATEGORIES.map(cat => (
                    <button
                        key={cat.value}
                        onClick={() => onCategoryChange(cat.value)}
                        className={`w-full flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm transition-colors ${
                            activeCategory === cat.value
                                ? 'bg-vault-accent text-white'
                                : 'text-vault-muted hover:bg-vault-bg hover:text-white'
                        }`}
                    >
                        <span>{cat.icon}</span>
                        <span>{cat.label}</span>
                    </button>
                ))}
            </nav>

            <div className="p-2 border-t border-vault-border space-y-0.5">
                <button
                    onClick={onSettings}
                    className="w-full flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm text-vault-muted hover:bg-vault-bg hover:text-white transition-colors"
                >
                    <span>⚙️</span>
                    <span>Einstellungen</span>
                </button>
                <button
                    onClick={onLock}
                    className="w-full flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm text-vault-muted hover:bg-red-900/30 hover:text-red-400 transition-colors"
                >
                    <span>🔒</span>
                    <span>Sperren</span>
                </button>
            </div>
        </aside>
    );
}
```

- [ ] **Step 2: `src/components/EntryList.tsx` erstellen**

```tsx
import type { ItemWithPayload } from '../types';

interface Props {
    items: ItemWithPayload[];
    selectedId: string | null;
    onSelect: (item: ItemWithPayload) => void;
    onAdd: () => void;
    searchQuery: string;
}

const CATEGORY_ICON: Record<string, string> = {
    login: '🔑',
    card: '💳',
    note: '📝',
    identity: '👤',
};

export default function EntryList({ items, selectedId, onSelect, onAdd, searchQuery }: Props) {
    const filtered = items.filter(item =>
        item.payload.title.toLowerCase().includes(searchQuery.toLowerCase()) ||
        item.payload.username.toLowerCase().includes(searchQuery.toLowerCase()) ||
        item.payload.url.toLowerCase().includes(searchQuery.toLowerCase())
    );

    return (
        <div className="w-72 border-r border-vault-border flex flex-col h-screen bg-vault-bg">
            <div className="p-3 border-b border-vault-border flex items-center justify-between">
                <span className="text-sm text-vault-muted">{filtered.length} Einträge</span>
                <button
                    onClick={onAdd}
                    className="text-xs bg-vault-accent hover:bg-indigo-500 text-white px-2.5 py-1 rounded-lg transition-colors"
                >
                    + Neu
                </button>
            </div>

            <div className="flex-1 overflow-y-auto">
                {filtered.length === 0 ? (
                    <div className="p-6 text-center text-vault-muted text-sm">
                        {searchQuery ? 'Keine Ergebnisse' : 'Noch keine Einträge'}
                    </div>
                ) : (
                    filtered.map(item => (
                        <button
                            key={item.id}
                            onClick={() => onSelect(item)}
                            className={`w-full text-left px-4 py-3 border-b border-vault-border transition-colors ${
                                selectedId === item.id
                                    ? 'bg-vault-surface border-l-2 border-l-vault-accent'
                                    : 'hover:bg-vault-surface/50'
                            }`}
                        >
                            <div className="flex items-center gap-2.5">
                                <span className="text-base">{CATEGORY_ICON[item.category] ?? '📄'}</span>
                                <div className="min-w-0">
                                    <p className="text-sm font-medium text-white truncate">
                                        {item.payload.title || '(kein Titel)'}
                                    </p>
                                    <p className="text-xs text-vault-muted truncate">
                                        {item.payload.username || item.payload.url || item.category}
                                    </p>
                                </div>
                            </div>
                        </button>
                    ))
                )}
            </div>
        </div>
    );
}
```

- [ ] **Step 3: `src/components/EntryDetail.tsx` erstellen**

```tsx
import { useState, useEffect } from 'react';
import { invoke } from '@tauri-apps/api/core';
import type { ItemWithPayload, ItemPayload } from '../types';
import { copyWithAutoClear } from '../utils/clipboard';
import { checkStrength } from '../utils/strength';

interface Props {
    item: ItemWithPayload | null;
    onSaved: () => void;
    onDeleted: () => void;
    onNew: boolean;
    category: string;
}

const EMPTY_PAYLOAD: ItemPayload = {
    title: '', username: '', password: '', url: '', notes: '', fields: []
};

export default function EntryDetail({ item, onSaved, onDeleted, onNew, category }: Props) {
    const [payload, setPayload] = useState<ItemPayload>(EMPTY_PAYLOAD);
    const [showPw, setShowPw] = useState(false);
    const [copying, setCopying] = useState(false);
    const [saving, setSaving] = useState(false);
    const [editing, setEditing] = useState(false);

    useEffect(() => {
        if (item) {
            setPayload(item.payload);
            setEditing(false);
            setShowPw(false);
        } else if (onNew) {
            setPayload(EMPTY_PAYLOAD);
            setEditing(true);
        }
    }, [item, onNew]);

    const strength = checkStrength(payload.password);

    async function handleSave() {
        setSaving(true);
        try {
            if (item && !onNew) {
                await invoke('update_item', { id: item.id, payload });
            } else {
                await invoke('add_item', { category, payload });
            }
            setEditing(false);
            onSaved();
        } catch (err) {
            console.error(err);
        } finally {
            setSaving(false);
        }
    }

    async function handleDelete() {
        if (!item) return;
        if (!confirm(`"${item.payload.title}" wirklich löschen?`)) return;
        await invoke('delete_item', { id: item.id });
        onDeleted();
    }

    async function handleCopyPw() {
        await copyWithAutoClear(payload.password);
        setCopying(true);
        setTimeout(() => setCopying(false), 2000);
    }

    function field(label: string, field: keyof ItemPayload, type = 'text') {
        const value = payload[field] as string;
        return (
            <div>
                <label className="block text-xs text-vault-muted mb-1">{label}</label>
                {editing ? (
                    <input
                        type={type}
                        value={value}
                        onChange={e => setPayload(p => ({ ...p, [field]: e.target.value }))}
                        className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-sm text-white focus:outline-none focus:border-vault-accent"
                    />
                ) : (
                    <p className="text-sm text-white py-2">{value || <span className="text-vault-muted">—</span>}</p>
                )}
            </div>
        );
    }

    if (!item && !onNew) {
        return (
            <div className="flex-1 flex items-center justify-center text-vault-muted text-sm">
                Eintrag auswählen oder neu erstellen
            </div>
        );
    }

    return (
        <div className="flex-1 overflow-y-auto p-6">
            <div className="max-w-lg space-y-4">
                <div className="flex items-center justify-between">
                    <h2 className="text-lg font-semibold text-white">
                        {onNew ? 'Neuer Eintrag' : (editing ? 'Bearbeiten' : payload.title || '(kein Titel)')}
                    </h2>
                    <div className="flex gap-2">
                        {!editing && !onNew && (
                            <>
                                <button onClick={() => setEditing(true)}
                                    className="text-xs border border-vault-border text-vault-muted hover:text-white px-3 py-1.5 rounded-lg transition-colors">
                                    Bearbeiten
                                </button>
                                <button onClick={handleDelete}
                                    className="text-xs border border-vault-danger/50 text-vault-danger hover:bg-red-900/20 px-3 py-1.5 rounded-lg transition-colors">
                                    Löschen
                                </button>
                            </>
                        )}
                        {(editing || onNew) && (
                            <button onClick={handleSave} disabled={saving}
                                className="text-xs bg-vault-accent hover:bg-indigo-500 text-white px-3 py-1.5 rounded-lg transition-colors disabled:opacity-50">
                                {saving ? 'Speichern…' : 'Speichern'}
                            </button>
                        )}
                    </div>
                </div>

                {field('Titel', 'title')}
                {field('Benutzername', 'username')}

                <div>
                    <label className="block text-xs text-vault-muted mb-1">Passwort</label>
                    <div className="flex gap-2">
                        {editing ? (
                            <div className="flex-1">
                                <input
                                    type={showPw ? 'text' : 'password'}
                                    value={payload.password}
                                    onChange={e => setPayload(p => ({ ...p, password: e.target.value }))}
                                    className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-sm text-white font-mono focus:outline-none focus:border-vault-accent"
                                />
                                {payload.password && (
                                    <div className="flex gap-1 mt-1.5">
                                        {[0,1,2,3,4].map(i => (
                                            <div key={i} className={`h-1 flex-1 rounded-full ${i <= strength.score ? strength.color : 'bg-vault-border'}`} />
                                        ))}
                                    </div>
                                )}
                            </div>
                        ) : (
                            <p className="flex-1 text-sm text-white font-mono py-2">
                                {showPw ? payload.password : '••••••••••••'}
                            </p>
                        )}
                        <button onClick={() => setShowPw(s => !s)}
                            className="text-xs border border-vault-border text-vault-muted hover:text-white px-2.5 py-1.5 rounded-lg transition-colors self-start">
                            {showPw ? 'Verbergen' : 'Anzeigen'}
                        </button>
                        {!editing && (
                            <button onClick={handleCopyPw}
                                className="text-xs border border-vault-border text-vault-muted hover:text-white px-2.5 py-1.5 rounded-lg transition-colors self-start">
                                {copying ? '✓ Kopiert' : 'Kopieren'}
                            </button>
                        )}
                    </div>
                </div>

                {field('URL', 'url', 'url')}
                
                <div>
                    <label className="block text-xs text-vault-muted mb-1">Notizen</label>
                    {editing ? (
                        <textarea
                            value={payload.notes}
                            onChange={e => setPayload(p => ({ ...p, notes: e.target.value }))}
                            rows={4}
                            className="w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-sm text-white focus:outline-none focus:border-vault-accent resize-none"
                        />
                    ) : (
                        <p className="text-sm text-white py-2 whitespace-pre-wrap">
                            {payload.notes || <span className="text-vault-muted">—</span>}
                        </p>
                    )}
                </div>
            </div>
        </div>
    );
}
```

- [ ] **Step 4: `src/components/VaultView.tsx` erstellen**

```tsx
import { useState, useEffect, useCallback } from 'react';
import { invoke } from '@tauri-apps/api/core';
import type { ItemWithPayload, ItemCategory } from '../types';
import Sidebar from './Sidebar';
import EntryList from './EntryList';
import EntryDetail from './EntryDetail';
import Settings from './Settings';

interface Props {
    onLock: () => void;
}

export default function VaultView({ onLock }: Props) {
    const [items, setItems] = useState<ItemWithPayload[]>([]);
    const [selectedItem, setSelectedItem] = useState<ItemWithPayload | null>(null);
    const [activeCategory, setActiveCategory] = useState<ItemCategory | 'all'>('all');
    const [searchQuery, setSearchQuery] = useState('');
    const [showSettings, setShowSettings] = useState(false);
    const [addingNew, setAddingNew] = useState(false);

    const loadItems = useCallback(async () => {
        const cat = activeCategory === 'all' ? undefined : activeCategory;
        const data = await invoke<ItemWithPayload[]>('get_items', { category: cat ?? null });
        setItems(data);
    }, [activeCategory]);

    useEffect(() => { loadItems(); }, [loadItems]);

    async function handleLock() {
        await invoke('lock_vault');
        onLock();
    }

    function handleSaved() {
        setAddingNew(false);
        loadItems();
    }

    function handleDeleted() {
        setSelectedItem(null);
        loadItems();
    }

    if (showSettings) {
        return <Settings onBack={() => setShowSettings(false)} />;
    }

    return (
        <div className="flex h-screen overflow-hidden">
            <Sidebar
                activeCategory={activeCategory}
                onCategoryChange={cat => { setActiveCategory(cat); setSelectedItem(null); setAddingNew(false); }}
                searchQuery={searchQuery}
                onSearchChange={setSearchQuery}
                onSettings={() => setShowSettings(true)}
                onLock={handleLock}
            />
            <EntryList
                items={items}
                selectedId={selectedItem?.id ?? null}
                onSelect={item => { setSelectedItem(item); setAddingNew(false); }}
                onAdd={() => { setSelectedItem(null); setAddingNew(true); }}
                searchQuery={searchQuery}
            />
            <EntryDetail
                item={selectedItem}
                onSaved={handleSaved}
                onDeleted={handleDeleted}
                onNew={addingNew}
                category={activeCategory === 'all' ? 'login' : activeCategory}
            />
        </div>
    );
}
```

- [ ] **Step 5: Commit**

```bash
git add src/components/Sidebar.tsx src/components/EntryList.tsx src/components/EntryDetail.tsx src/components/VaultView.tsx
git commit -m "feat: add VaultView, Sidebar, EntryList, EntryDetail components"
```

---

## Task 11: Settings

**Files:**
- Create: `src/components/SecuritySettings.tsx`
- Create: `src/components/SyncSettings.tsx`
- Create: `src/components/Settings.tsx`

- [ ] **Step 1: `src/components/SecuritySettings.tsx` erstellen**

```tsx
import { useState } from 'react';
import { invoke } from '@tauri-apps/api/core';

export default function SecuritySettings() {
    const [lockTimeout, setLockTimeout] = useState(5);
    const [syncing, setSyncing] = useState(false);
    const [message, setMessage] = useState('');

    async function handleLocalBackup() {
        setSyncing(true);
        setMessage('');
        try {
            await invoke('sync_local');
            setMessage('✓ Backup erfolgreich erstellt');
        } catch (err) {
            setMessage(`Fehler: ${String(err)}`);
        } finally {
            setSyncing(false);
        }
    }

    return (
        <div className="space-y-6">
            <div>
                <h3 className="text-sm font-medium text-white mb-3">Auto-Lock</h3>
                <div className="flex items-center gap-3">
                    <input
                        type="range"
                        min={1}
                        max={60}
                        value={lockTimeout}
                        onChange={e => setLockTimeout(Number(e.target.value))}
                        className="flex-1 accent-indigo-500"
                    />
                    <span className="text-sm text-white w-24 text-right">
                        {lockTimeout} {lockTimeout === 1 ? 'Minute' : 'Minuten'}
                    </span>
                </div>
                <p className="text-xs text-vault-muted mt-1">
                    Vault wird automatisch nach Inaktivität gesperrt.
                </p>
            </div>

            <div>
                <h3 className="text-sm font-medium text-white mb-3">Manuelles Backup</h3>
                <button
                    onClick={handleLocalBackup}
                    disabled={syncing}
                    className="bg-vault-accent hover:bg-indigo-500 disabled:opacity-50 text-white text-sm font-medium px-4 py-2 rounded-lg transition-colors"
                >
                    {syncing ? 'Backup läuft…' : 'Lokales Backup erstellen'}
                </button>
                {message && (
                    <p className={`text-sm mt-2 ${message.startsWith('✓') ? 'text-green-400' : 'text-red-400'}`}>
                        {message}
                    </p>
                )}
            </div>
        </div>
    );
}
```

- [ ] **Step 2: `src/components/SyncSettings.tsx` erstellen**

```tsx
import { useState } from 'react';
import { invoke } from '@tauri-apps/api/core';

export default function SyncSettings() {
    const [host, setHost] = useState('');
    const [port, setPort] = useState('22');
    const [username, setUsername] = useState('');
    const [remotePath, setRemotePath] = useState('/backup/sd-vault');
    const [authType, setAuthType] = useState<'KeyFile' | 'Password'>('KeyFile');
    const [keyPath, setKeyPath] = useState('');
    const [password, setPassword] = useState('');
    const [saving, setSaving] = useState(false);
    const [syncing, setSyncing] = useState(false);
    const [message, setMessage] = useState('');

    async function handleSave() {
        setSaving(true);
        setMessage('');
        try {
            const auth = authType === 'KeyFile'
                ? { type: 'KeyFile', key_path: keyPath }
                : { type: 'Password', password };
            await invoke('save_sftp_config', {
                sftpConfig: {
                    host, port: parseInt(port), username, remote_path: remotePath, auth
                }
            });
            setMessage('✓ Konfiguration gespeichert');
        } catch (err) {
            setMessage(`Fehler: ${String(err)}`);
        } finally {
            setSaving(false);
        }
    }

    async function handleSync() {
        setSyncing(true);
        setMessage('');
        try {
            await invoke('sync_sftp');
            setMessage('✓ SFTP-Sync erfolgreich');
        } catch (err) {
            setMessage(`Fehler: ${String(err)}`);
        } finally {
            setSyncing(false);
        }
    }

    const inputClass = "w-full bg-vault-bg border border-vault-border rounded-lg px-3 py-2 text-sm text-white focus:outline-none focus:border-vault-accent";

    return (
        <div className="space-y-4">
            <div className="grid grid-cols-2 gap-3">
                <div>
                    <label className="block text-xs text-vault-muted mb-1">Host</label>
                    <input value={host} onChange={e => setHost(e.target.value)} className={inputClass} placeholder="backup.example.com" />
                </div>
                <div>
                    <label className="block text-xs text-vault-muted mb-1">Port</label>
                    <input value={port} onChange={e => setPort(e.target.value)} className={inputClass} placeholder="22" />
                </div>
            </div>

            <div>
                <label className="block text-xs text-vault-muted mb-1">Benutzername</label>
                <input value={username} onChange={e => setUsername(e.target.value)} className={inputClass} />
            </div>

            <div>
                <label className="block text-xs text-vault-muted mb-1">Remote-Pfad</label>
                <input value={remotePath} onChange={e => setRemotePath(e.target.value)} className={inputClass} />
            </div>

            <div>
                <label className="block text-xs text-vault-muted mb-1">Authentifizierung</label>
                <div className="flex gap-3 mb-2">
                    {(['KeyFile', 'Password'] as const).map(t => (
                        <label key={t} className="flex items-center gap-2 cursor-pointer">
                            <input type="radio" checked={authType === t} onChange={() => setAuthType(t)} className="accent-indigo-500" />
                            <span className="text-sm text-vault-muted">{t === 'KeyFile' ? 'SSH-Key (empfohlen)' : 'Passwort'}</span>
                        </label>
                    ))}
                </div>
                {authType === 'KeyFile' ? (
                    <input value={keyPath} onChange={e => setKeyPath(e.target.value)} className={inputClass} placeholder="~/.ssh/id_ed25519" />
                ) : (
                    <input type="password" value={password} onChange={e => setPassword(e.target.value)} className={inputClass} placeholder="SSH-Passwort" />
                )}
            </div>

            {message && (
                <p className={`text-sm ${message.startsWith('✓') ? 'text-green-400' : 'text-red-400'}`}>{message}</p>
            )}

            <div className="flex gap-3">
                <button onClick={handleSave} disabled={saving}
                    className="bg-vault-accent hover:bg-indigo-500 disabled:opacity-50 text-white text-sm font-medium px-4 py-2 rounded-lg transition-colors">
                    {saving ? 'Speichern…' : 'Konfiguration speichern'}
                </button>
                <button onClick={handleSync} disabled={syncing}
                    className="border border-vault-border text-vault-muted hover:text-white text-sm font-medium px-4 py-2 rounded-lg transition-colors disabled:opacity-50">
                    {syncing ? 'Sync läuft…' : 'Jetzt synchronisieren'}
                </button>
            </div>
        </div>
    );
}
```

- [ ] **Step 3: `src/components/Settings.tsx` erstellen**

```tsx
import SecuritySettings from './SecuritySettings';
import SyncSettings from './SyncSettings';

interface Props {
    onBack: () => void;
}

type Tab = 'security' | 'sync';

import { useState } from 'react';

export default function Settings({ onBack }: Props) {
    const [tab, setTab] = useState<Tab>('security');

    return (
        <div className="flex h-screen overflow-hidden">
            <aside className="w-56 bg-vault-surface border-r border-vault-border flex flex-col">
                <div className="p-4 border-b border-vault-border">
                    <button onClick={onBack} className="flex items-center gap-2 text-vault-muted hover:text-white text-sm transition-colors">
                        ← Zurück
                    </button>
                    <h2 className="font-semibold text-white mt-3">Einstellungen</h2>
                </div>
                <nav className="p-2 space-y-0.5">
                    {([
                        { value: 'security', label: 'Sicherheit', icon: '🔒' },
                        { value: 'sync', label: 'SFTP-Sync', icon: '☁️' },
                    ] as { value: Tab; label: string; icon: string }[]).map(t => (
                        <button key={t.value} onClick={() => setTab(t.value)}
                            className={`w-full flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm transition-colors ${
                                tab === t.value ? 'bg-vault-accent text-white' : 'text-vault-muted hover:bg-vault-bg hover:text-white'
                            }`}>
                            <span>{t.icon}</span>
                            <span>{t.label}</span>
                        </button>
                    ))}
                </nav>
            </aside>

            <main className="flex-1 p-8 overflow-y-auto">
                <h3 className="text-lg font-semibold text-white mb-6">
                    {tab === 'security' ? 'Sicherheit & Backup' : 'SFTP-Sync konfigurieren'}
                </h3>
                {tab === 'security' ? <SecuritySettings /> : <SyncSettings />}
            </main>
        </div>
    );
}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/Settings.tsx src/components/SecuritySettings.tsx src/components/SyncSettings.tsx
git commit -m "feat: add Settings, SecuritySettings, and SyncSettings components"
```

---

## Task 12: App.tsx & Final Integration

**Files:**
- Create: `src/App.tsx`

- [ ] **Step 1: `src/App.tsx` erstellen**

```tsx
import { useState, useEffect } from 'react';
import { invoke } from '@tauri-apps/api/core';
import type { AppScreen, VaultMeta } from './types';
import LockScreen from './components/LockScreen';
import FirstRunSetup from './components/FirstRunSetup';
import VaultView from './components/VaultView';

export default function App() {
    const [screen, setScreen] = useState<AppScreen>('loading');
    const [vaultExists, setVaultExists] = useState(false);
    const [_meta, setMeta] = useState<VaultMeta | null>(null);

    useEffect(() => {
        invoke<boolean>('vault_exists').then(exists => {
            setVaultExists(exists);
            setScreen(exists ? 'lock' : 'first-run');
        });
    }, []);

    function handleUnlocked(meta: VaultMeta) {
        setMeta(meta);
        setScreen('vault');
    }

    function handleVaultCreated(meta: VaultMeta) {
        setVaultExists(true);
        setMeta(meta);
        setScreen('vault');
    }

    function handleLocked() {
        setMeta(null);
        setScreen('lock');
    }

    if (screen === 'loading') {
        return (
            <div className="min-h-screen bg-vault-bg flex items-center justify-center">
                <div className="text-vault-muted text-sm">Wird geladen…</div>
            </div>
        );
    }

    if (screen === 'first-run') {
        return <FirstRunSetup onVaultCreated={handleVaultCreated} />;
    }

    if (screen === 'lock') {
        return (
            <LockScreen
                onUnlocked={handleUnlocked}
                onCreateNew={() => setScreen('first-run')}
                vaultExists={vaultExists}
            />
        );
    }

    if (screen === 'vault') {
        return <VaultView onLock={handleLocked} />;
    }

    return null;
}
```

- [ ] **Step 2: TypeScript-Kompilierung prüfen**

```bash
pnpm build
```

Expected: TypeScript kompiliert, Vite bundled ohne Fehler

- [ ] **Step 3: Entwicklungs-Server starten und UI testen**

```bash
pnpm tauri dev
```

Manuell testen:
- [ ] App startet, zeigt FirstRunSetup (kein Vault vorhanden)
- [ ] Master-Passwort eingeben, Vault erstellen → Emergency Kit PDF Download
- [ ] Nach Bestätigung: VaultView erscheint
- [ ] Neuen Login-Eintrag erstellen, speichern, wieder öffnen
- [ ] Passwort kopieren → 30s später aus Clipboard verschwunden
- [ ] Sperren-Button → LockScreen erscheint
- [ ] Unlock mit Master PW + Secret Key → Vault öffnet sich mit gespeicherten Einträgen
- [ ] Settings → SFTP konfigurieren → Konfiguration speichern

- [ ] **Step 4: Finaler Commit**

```bash
git add src/App.tsx
git commit -m "feat: add App.tsx router and complete SD-Vault frontend"
```

---

## Self-Review Ergebnis

### Spec Coverage Check
| Spec-Anforderung | Task |
|---|---|
| Dual-Key System (Master PW + Secret Key) | Task 2 (crypto.rs), Task 7 (create_vault/unlock_vault) |
| Argon2id m=256MB, t=3, p=4 | Task 2 (crypto.rs Konstanten) |
| XChaCha20-Poly1305 per Eintrag | Task 2 (encrypt/decrypt), Task 3 (db.rs) |
| BLAKE3 Kombination der Inputs | Task 2 (derive_master_key) |
| HKDF für SQLCipher-Key + Entry-Key | Task 2 (derive_sqlcipher_key/derive_entry_key) |
| SQLCipher für DB-Datei | Task 3 (open_db PRAGMA key) |
| zeroize für alle Keys | Task 7 (Zeroizing<[u8;32]>, drop in lock_vault) |
| Emergency Kit PDF | Task 4 (emergency_kit.rs), Task 9 (FirstRunSetup) |
| PDF-Download erzwungen vor Vault-Erstellung | Task 9 (pdfSaved State + Confirm Step) |
| Vault in Dokumente-Ordner | Task 7 (get_vault_dir via dirs::document_dir) |
| Local Backup (max. 10) | Task 5 (local.rs + prune) |
| SFTP vollständig | Task 6 (sftp.rs + config.rs) |
| Google Drive Stub | Task 5 (google_drive.rs) |
| sync_config.toml neben vault.db | Task 5 (config.rs), Task 7 (save_sftp_config) |
| Bootstrap-Problem (Salt) | Task 7 (vault.salt neben vault.db) |
| Clipboard auto-clear 30s | Task 8 (utils/clipboard.ts) |
| Passwort-Stärke-Anzeige | Task 8 (utils/strength.ts), Task 10/11 (EntryDetail/FirstRun) |
| Dark Mode UI | Task 1 (tailwind.config.js vault-Farben), alle Komponenten |
